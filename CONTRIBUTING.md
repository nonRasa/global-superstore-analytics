# راهنمای همکاری تیم

## گردش‌کار Git

شاخه `main` باید همیشه قابل ارائه باشد. هر کار در یک شاخه جدا انجام می‌شود:

- `feature/powerbi-star-schema`
- `feature/statistics-discount`
- `feature/ml-profit-regression`
- `feature/ml-shipmode-classification`
- `feature/dashboard`
- `docs/presentation`

برای هر تغییر:

1. آخرین نسخه `main` را دریافت کنید.
2. یک شاخه کوتاه و مشخص بسازید.
3. تغییرات کوچک و مرتبط را commit کنید.
4. Pull Request باز کنید و نتیجه تست/اعتبارسنجی را بنویسید.
5. حداقل یک نفر دیگر تغییر را بازبینی کند.

## قرارداد نام‌گذاری

- نام پوشه‌ها و فایل‌ها: انگلیسی، حروف کوچک و `snake_case`
- نوت‌بوک‌ها: شماره‌دار، مانند `03_profit_regression.ipynb`
- commitها: کوتاه و امری، مانند `feat: add profit baseline model`
- نام جدول‌ها: `FactSales`, `DimDate`, `DimProduct`, ...
- Measureهای Power BI: نام خوانا و پوشه‌بندی‌شده بر اساس موضوع

## قواعد داده و خروجی

- داده خام، رمز عبور، فایل `.env` و مدل‌های حجیم commit نشوند.
- فایل اصلی Power BI در `powerbi/` نگهداری شود؛ فقط یک نفر در هر لحظه آن را ویرایش کند تا conflict ایجاد نشود.
- M Queryها و DAXهای مهم به صورت متن در `powerbi/power-query/` و `powerbi/dax/` نیز ذخیره شوند تا تغییرات قابل مرور باشند.
- seed، تقسیم train/test و نسخه کتابخانه‌ها در هر تحلیل ثبت شوند.
- نتیجه هر آزمایش باید شامل روش، فرض‌ها، معیارها و تفسیر کسب‌وکاری باشد.

## تعریف Done

یک کار زمانی کامل است که کد/گزارش، خروجی قابل بازتولید، مستندات کوتاه و بررسی هم‌تیمی را داشته باشد.

